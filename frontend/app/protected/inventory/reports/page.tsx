import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Inventory Reports',
  description: 'Manage inventory reports.',
};

export default function InventoryReportsPage() {
  return <h1>Inventory Reports</h1>;
}
