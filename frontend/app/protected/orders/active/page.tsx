import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Active Orders',
  description: 'Manage active orders.',
};

export default function ActiveOrdersPage() {
  return <h1>Active Orders</h1>;
}
