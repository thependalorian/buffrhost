import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Property Settings',
  description: 'Manage property settings.',
};

export default function PropertySettingsPage() {
  return <h1>Property Settings</h1>;
}
