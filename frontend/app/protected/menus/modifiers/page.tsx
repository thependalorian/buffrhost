import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Menu Modifiers',
  description: 'Manage menu modifiers.',
};

export default function MenuModifiersPage() {
  return <h1>Menu Modifiers</h1>;
}
