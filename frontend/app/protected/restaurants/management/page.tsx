import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Restaurant Management',
  description: 'Manage restaurants.',
};

export default function RestaurantManagementPage() {
  return <h1>Restaurant Management</h1>;
}
