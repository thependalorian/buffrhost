import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Restaurant Analytics',
  description: 'View restaurant analytics.',
};

export default function RestaurantAnalyticsPage() {
  return <h1>Restaurant Analytics</h1>;
}
