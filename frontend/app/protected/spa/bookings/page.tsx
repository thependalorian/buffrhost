import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SPA Bookings',
  description: 'Manage spa appointments and reservations.',
};

export default function SPABookingsPage() {
  return <h1>SPA Bookings</h1>;
}
