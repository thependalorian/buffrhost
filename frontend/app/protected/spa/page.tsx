import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SPA Management',
  description: 'Manage spa services, bookings, and staff.',
};

export default function SPAPage() {
  return <h1>SPA Management</h1>;
}
