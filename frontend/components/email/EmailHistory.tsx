import React from "react";

const EmailHistory: React.FC = () => {
  return (
    <div className="email-history">
      <h2>Email History</h2>
      <p>
        This component will display a log of all emails sent to a Buffr Host
        user.
      </p>
      {/* Add email history UI here */}
    </div>
  );
};

export default EmailHistory;
